name: a name for my workflow

on:
    push:
        branches: main
jobs:
    build-deploy:
        name: something
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v5
                with:
                    python-version: '3.13' 
            - run: python my_script.py
